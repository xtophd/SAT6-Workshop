#!/bin/bash

echo "Activation Key Content Auto Enablement"

hammer activation-key content-override \
--name "{{ myActivationKeyName_1 }}" \
--content-label rhel-7-server-satellite-tools-{{ g_satelliteVersion }}-rpms \
--value 1 \
--organization "{{ myOrganization }}"

hammer activation-key content-override \
--name "{{ myActivationKeyName_2 }}" \
--content-label rhel-7-server-satellite-tools-{{ g_satelliteVersion }}-rpms \
--value 1 \
--organization "{{ myOrganization }}"
