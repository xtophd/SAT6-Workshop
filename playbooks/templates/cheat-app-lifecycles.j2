#!/bin/bash

hammer lifecycle-environment create \
  --organization "{{ workshop_vars.myOrganization }}" \
  --name "{{ workshop_vars.myLifecycleName_1 }}" \
  --description "" \
  --prior "Library"

hammer lifecycle-environment create \
  --organization "{{ workshop_vars.myOrganization }}" \
  --name "{{ workshop_vars.myLifecycleName_2 }}" \
  --description "" \
  --prior "{{ workshop_vars.myLifecycleName_1 }}"

hammer lifecycle-environment create \
  --organization "{{ workshop_vars.myOrganization }}" \
  --name "{{ workshop_vars.myLifecycleName_3 }}" \
  --description "" \
  --prior "{{ workshop_vars.myLifecycleName_2 }}"
